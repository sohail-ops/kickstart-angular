trigger:
- main

pool:
  vmImage: 'ubuntu-latest'

steps:
- task: NodeTool@0
  inputs:
    version: 'lts'
  displayName: 'Install Node.js'

- script:
    node --version
    npm install
  displayName: 'npm install'

- script: |
    npm run build
  displayName: 'npm build'